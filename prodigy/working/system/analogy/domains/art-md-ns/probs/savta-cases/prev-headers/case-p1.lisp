
(setf result '(
   (time 0.283)
   (nodes 12)
   (exhaustedp nil)
   (solutionp t)
   (solution-length 2)
   (solution ((a1-1) (a2-1)))))

(setf goal '((g1)))
(setf initial-state '((i1)))

(setf case-objects '(nil))
(setf insts-to-vars '(
   
))

(setf footprint-by-goal '(
   ((g1) (i1))))
