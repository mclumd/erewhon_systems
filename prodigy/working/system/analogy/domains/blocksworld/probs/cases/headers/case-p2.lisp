
(setf result '(
   (time 0.034)
   (nodes 8)
   (exhaustedp nil)
   (solutionp t)
   (solution-length 1)
   (solution ((pick-up b)))))

(setf problem-solved 
   "~/Research/Prodigy/analogy/domains/blocksworld/probs/p2")
(setf goal '((holding b)))

(setf case-objects '((a b c object)))

(setf insts-to-vars '(
   (a . <object18>) (b . <object4>) (c . <object90>) 
))

(setf footprint-by-goal '(
   ((holding b) (arm-empty) (on-table b) (clear b))))
