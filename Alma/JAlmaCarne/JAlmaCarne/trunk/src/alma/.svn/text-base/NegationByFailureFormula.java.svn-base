package alma;

public class NegationByFailureFormula extends ComplexNode {
	NegationByFailureFormula() {
	}

	public FormulaNode getChild() {
		return children.get(0);
	}

	public String toString() {
		assert (children.size() == 1);
		return "!(" + children.get(0) + ")";
	}
}